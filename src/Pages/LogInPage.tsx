import React from "react";
import Login from "../Components/Login";

const LogInPage = () => {
  return (
    <div className="bg-red-400 h-[100vh] flex items-center justify-center p-10">
      <Login />
    </div>
  );
};

export default LogInPage;
